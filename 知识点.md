| **问题（面试官视角）**                                       | **答案（技术要点）**                                         | **形象解释**                                                 |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **1. 什么是 Peer Dependencies 问题？您如何解决 Vite 插件版本冲突？** | Peer Dependencies 是指一个包（如 `@vitejs/plugin-react`）依赖于另一个宿主包（如 `vite`）的特定版本。解决冲突的最好办法是**升级插件** (`@vitejs/plugin-react`)，使其兼容当前项目使用的 Vite 版本。 | 就像你买了一个 **手机壳**（插件），但它只适配 iPhone 14 Pro（旧版 Vite）。如果你的手机已经升级到了 iPhone 15 Pro（新版 Vite），你就必须升级手机壳到兼容 15 Pro 的版本才能使用。 |
| **2. 在 Node.js 项目中，`package.json` 的核心作用是什么？`npm run dev` 如何工作？** | `package.json` 是项目元数据和配置清单，定义了依赖、版本和启动脚本。`npm run dev` 通过查找 `scripts` 字段中的 `dev` 键，将对应的值（如 `nodemon server.js`）作为终端命令来执行，并临时将 `node_modules/.bin` 路径注入到环境变量中，从而可以直接调用 `nodemon` 等工具。 | `package.json` 就像项目的 **说明书和快捷键列表**。`npm run dev` 就像你按下了一个 **宏命令** 按钮，它自动帮你输入并运行了一长串复杂的指令。 |
| **3. Node.js 的模块化机制（`module.exports` 和 `require`）与面向对象编程中的 Public 访问控制有什么相似之处？** | **相似点**：`module.exports` 类似于 OOP 中的 `public` 访问修饰符，它定义了模块对外部暴露的公共接口；未导出的变量则默认为私有（`private`）。**`require`** 类似于引入头文件，用于消费这些公共接口。 | 就像你建了一栋 **积木房子**（模块）。`module.exports` 是你决定 **留下来的门窗**（公共接口）。房子的内部家具（私有变量）和结构，外面的人看不到。`require` 就是拿着钥匙来开门窗。 |
| **4. 为什么要使用 Express 服务器？它在前后端分离架构中的角色是什么？** | Express 因其 **轻量级、灵活和语言统一**（与 React/Node.js 保持一致）而被选用。它在架构中充当 **API 提供者** 的角色，负责处理业务逻辑、安全鉴权、数据存储（模拟或连接数据库），并向前端提供数据接口（如 `/api/history`）。 | Express 就像餐厅的 **厨房和服务窗口**。React（顾客）通过服务窗口（API 接口）点菜（发请求），厨房（Express）负责处理食材（查询数据库/生成数据），并把做好的菜（JSON 数据）递交给顾客。 |

| **问题（面试官视角）**                                       | **答案（技术要点）**                                         | **形象解释**                                                 |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **5. 解释 Express 中 `app.get()` 和 `app.listen()` 的根本区别是什么？** | `app.get()` 是用来定义 **路由规则** 的，它告诉 Express **如何处理** 某个 URL 路径下的 GET 请求。而 `app.listen()` 才是真正 **启动网络监听** 的命令，它绑定端口，并将应用程序投入到网络中，使其能接收外部请求。 | `app.get()` 就像你在家里（服务器）设置 **接待客人的规则**（例如，敲门 `/` 就给文本）。`app.listen()` 就像你打开了家里的 **总电源和门铃**，开始等待客人的到来。没有 `listen`，你的规则再多也无济于事。 |
| **6. 为什么在 Express 路由处理中，推荐使用 `return res.json()` 而不是单独使用 `res.json()`？** | `res.json()` 或 `res.send()` 等方法只是发送响应，**不会自动终止 JavaScript 函数的执行**。使用 `return` 可以立即终止函数执行，防止代码继续流转到后续逻辑，从而避免程序尝试发送 **第二次响应**（即著名的 `Can't set headers after they are sent` 错误）。 | 就像你给客户寄包裹。一旦你把包裹 **密封并交给邮递员**（`res.json()`），你就必须 **转身离开**（`return`）。如果你不离开，你可能会试图再塞一个东西到包裹里，这会导致邮局（Express）报错。 |
| **7. 为什么浏览器在地址栏输入 URL 默认是 GET 请求？**        | 这是 HTTP **协议的规范**。GET 方法的语义是 **“安全地获取资源”**，不会改变服务器状态（幂等性）。浏览器将地址栏输入或链接点击视为获取资源的行为，因此默认为 GET，以保障刷新、回退等操作的安全性。 | 就像你在图书馆里 **查看目录**。你只是想知道哪本书在哪里（获取信息），你不会因为查看目录而改变书架上的书（不会改变服务器状态）。 |

| **问题（面试官视角）**                                       | **答案（技术要点）**                                         | **形象解释**                                                 |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **8. 简述 Zustand `create` 函数中 `set` 的作用，如果不使用它，会有什么后果？** | `set` 函数是 Zustand **修改 Store 状态** 的唯一入口。它的作用是安全地更新 Store 内部的状态，并触发所有订阅了该状态的组件进行 **响应式重渲染**。如果不使用 `set`，状态只会改变在 JavaScript 内存中，**但组件无法感知到变化**，从而无法重渲染（应用失去响应性）。 | `set` 就像 Store 状态的 **广播按钮**。只有通过它更新状态，Store 才会向所有订阅者（组件）发送通知。如果你直接偷偷修改状态，组件将永远不知道数据变了，导致界面静止。 |
| **9. 为什么要使用 `useAuthStore((state) => state.setToken)` 这种选择器模式，而不是直接 `useAuthStore().setToken`？** | 这种选择器模式是 Zustand **性能优化** 的核心。通过 `(state) => state.setToken`，我们只订阅了 Store 中的 **Action 函数**。由于 Action 函数的引用在生命周期内是稳定的，因此即使 `token` 状态改变，组件也不会因为 Action 函数的引用没变而触发不必要的重渲染。 | 你的组件在 Store 面前就像一个 **只点菜的客人**。使用选择器，你告诉服务员：“我只需要厨房的 **点菜热线**（`setToken` 函数）。” 这样，无论厨房里是换了厨师还是着火（其他状态改变），你都不必起身，只有热线电话变了你才需要关注，从而避免了不必要的起身（重渲染）。 |
| **10. 鉴权（Authentication）和前端路由守卫（Route Guard）之间的关系是什么？** | **鉴权是前提**，它发生在后端，目的是验证 **“你是谁？”**（验证凭证，颁发 Token）。**路由守卫是响应**，它发生在前端，目的是根据鉴权的结果（即 Token 是否存在/有效），决定是否允许用户进入受保护的页面，控制导航流。 | 鉴权是 **酒店前台验证身份并发放房卡**（Token）。路由守卫是 **房门上的读卡器**。它检查你是否有卡，如果有，就让你进入房间（`/dashboard`），没有就引导你回到前台（`/login`）。 |

11.React运行代码的简洁流程

你写代码 -> React 解析生成虚拟 DOM -> React 操作浏览器 API 转化为真实 DOM -> 浏览器生成 DOM 树和索引 -> 浏览器把画出来的东西给你看。