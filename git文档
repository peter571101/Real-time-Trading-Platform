git init
git add .
git commit -m "描述此次提交的内容"
前往github网站,点击new repository,只输个项目名称，然后创建
git branch //确保在主分支
git remote add origin https://github.com/peter571101/Real-time-Trading-Platform.git
git push -u origin master
git pull origin master
git checkout -b phase-4-websocket//创建分支
git add .
git commit -m "feat: 完成阶段四，实现 WebSocket 实时推送和 ECharts 动态更新"
git push -u origin phase-4-websocket//这一步完成后才能在github上查看到分支
git checkout master
git pull origin master
git merge phase-4-websocket
git push origin master